pipeline {
    agent any
    options {
  buildDiscarder logRotator(artifactDaysToKeepStr: '', artifactNumToKeepStr: '', daysToKeepStr: '1', numToKeepStr: '2')
             }
    stages {
        stage("BUILD") {
            steps {
                sleep 10
            }
        }
    }

}
post {
  always {
    
    // One or more steps need to be included within each condition's block.
  }
}
emailext body: 'Hi, Please find the Build status', recipientProviders: [developers()], subject: 'Build status Checking', to: 'nkallutla8@gmail.com'